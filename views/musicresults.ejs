       <div class="container">
       <div class="row">
        <div class="col s12 m8 offset-m2 center-align">
          <div class="card black darken-1 center-align">
            <div class="card-content white-text center-align">
              <span class="card-title"><h2>Top Results</h2></span><p>
	           <h5><% myData.response.artists.forEach(function(item){ %>
                <% var ID = item.name %><%= item.name %>
                <div class="row" style="text-align:center">
                  <form method="POST" action="/favorite">
                 <input class="name" type="hidden" value="<%-item.name%>" name="name"><br>
                 <input class="btn btn-info grey" type="submit" value="Add Artist">
	       </div>

			     </h5>
<% }) %>
        </div>
	</div>
</div>
</form>
